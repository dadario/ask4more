<h1>Editing task</h1>

<% form_for(@task) do |f| %>
  <%= f.error_messages %>
	<%= f.label :description, 'Description' %>:
	<%= f.text_field :description %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @task %> |
<%= link_to 'Back', tasks_path %>